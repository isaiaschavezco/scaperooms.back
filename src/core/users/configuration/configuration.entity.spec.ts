import { Configuration.Entity } from './configuration.entity';

describe('Configuration.Entity', () => {
  it('should be defined', () => {
    expect(new Configuration.Entity()).toBeDefined();
  });
});
