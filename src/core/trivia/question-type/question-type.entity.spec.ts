import { QuestionType.Entity } from './question-type.entity';

describe('QuestionType.Entity', () => {
  it('should be defined', () => {
    expect(new QuestionType.Entity()).toBeDefined();
  });
});
